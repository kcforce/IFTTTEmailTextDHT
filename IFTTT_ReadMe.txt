1. User Maker Service
2. Find key part of url : keep event name and maker api key https://maker.ifttt.com/use/KEYVALUE
3. Create a applet this then that for this : look for maker, create event name "sensor_update" for trigger
4. That use Gmail email (fill in: email, subject, values (Humidity, Temp(C), Temp(F) 

Create another applet
1. For next this then that
2. for this use maker set event id to event name "sensor_update"
3. for that use sms. use cell phone number, get pin finsh setup fill in message